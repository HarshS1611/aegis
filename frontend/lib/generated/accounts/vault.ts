/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getArrayDecoder, getArrayEncoder, getBytesDecoder, getBytesEncoder, getI64Decoder, getI64Encoder, getOptionDecoder, getOptionEncoder, getStructDecoder, getStructEncoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type Codec, type Decoder, type EncodedAccount, type Encoder, type FetchAccountConfig, type FetchAccountsConfig, type MaybeAccount, type MaybeEncodedAccount, type Option, type OptionOrNullable, type ReadonlyUint8Array } from '@solana/kit';
import { getRecoveryStateDecoder, getRecoveryStateEncoder, type RecoveryState, type RecoveryStateArgs } from '../types';

export const VAULT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([211, 8, 232, 43, 2, 152, 117, 119]);

export function getVaultDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(VAULT_DISCRIMINATOR); }

export type Vault = { discriminator: ReadonlyUint8Array; 
/** Current owner of the vault. Updated on successful rotation. */
owner: Address; 
/** PDA bump seed. */
bump: number; 
/** Registered guardian addresses. */
guardians: Array<Address>; 
/** Minimum guardian approvals required for rotation. */
threshold: number; 
/** Seconds before recovery can be triggered. */
inactivityWindow: bigint; 
/** Unix timestamp of last ping or vault initialisation. */
lastActivity: bigint; 
/** Current recovery state. */
recoveryState: RecoveryState; 
/** Proposed new owner address during recovery. None when Idle. */
proposedOwner: Option<Address>; 
/** Guardian addresses that have approved the current recovery. */
approvals: Array<Address>; 
/** Unix timestamp when recovery was initiated. Used for 7-day expiry. */
initiatedAt: bigint;  };

export type VaultArgs = { 
/** Current owner of the vault. Updated on successful rotation. */
owner: Address; 
/** PDA bump seed. */
bump: number; 
/** Registered guardian addresses. */
guardians: Array<Address>; 
/** Minimum guardian approvals required for rotation. */
threshold: number; 
/** Seconds before recovery can be triggered. */
inactivityWindow: number | bigint; 
/** Unix timestamp of last ping or vault initialisation. */
lastActivity: number | bigint; 
/** Current recovery state. */
recoveryState: RecoveryStateArgs; 
/** Proposed new owner address during recovery. None when Idle. */
proposedOwner: OptionOrNullable<Address>; 
/** Guardian addresses that have approved the current recovery. */
approvals: Array<Address>; 
/** Unix timestamp when recovery was initiated. Used for 7-day expiry. */
initiatedAt: number | bigint;  };

/** Gets the encoder for {@link VaultArgs} account data. */
export function getVaultEncoder(): Encoder<VaultArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['owner', getAddressEncoder()], ['bump', getU8Encoder()], ['guardians', getArrayEncoder(getAddressEncoder())], ['threshold', getU8Encoder()], ['inactivityWindow', getI64Encoder()], ['lastActivity', getI64Encoder()], ['recoveryState', getRecoveryStateEncoder()], ['proposedOwner', getOptionEncoder(getAddressEncoder())], ['approvals', getArrayEncoder(getAddressEncoder())], ['initiatedAt', getI64Encoder()]]), (value) => ({ ...value, discriminator: VAULT_DISCRIMINATOR }));
}

/** Gets the decoder for {@link Vault} account data. */
export function getVaultDecoder(): Decoder<Vault> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['owner', getAddressDecoder()], ['bump', getU8Decoder()], ['guardians', getArrayDecoder(getAddressDecoder())], ['threshold', getU8Decoder()], ['inactivityWindow', getI64Decoder()], ['lastActivity', getI64Decoder()], ['recoveryState', getRecoveryStateDecoder()], ['proposedOwner', getOptionDecoder(getAddressDecoder())], ['approvals', getArrayDecoder(getAddressDecoder())], ['initiatedAt', getI64Decoder()]]);
}

/** Gets the codec for {@link Vault} account data. */
export function getVaultCodec(): Codec<VaultArgs, Vault> {
    return combineCodec(getVaultEncoder(), getVaultDecoder());
}

export function decodeVault<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<Vault, TAddress>;
export function decodeVault<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<Vault, TAddress>;
export function decodeVault<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<Vault, TAddress> | MaybeAccount<Vault, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getVaultDecoder());
}

export async function fetchVault<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Vault, TAddress>> {
  const maybeAccount = await fetchMaybeVault(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeVault<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Vault, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeVault(maybeAccount);
}

export async function fetchAllVault(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Vault>[]> {
  const maybeAccounts = await fetchAllMaybeVault(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeVault(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Vault>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeVault(maybeAccount));
}